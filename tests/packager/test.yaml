AWSTemplateFormatVersion: '2010-09-09'
Description: Packager test
Parameters:
  RuntimeVersion:
    Type: String
    Default: python3.6
Resources:
  ApiGateway:
    Type: AWS::ApiGateway::RestApi
    &492464927 Properties:
      BodyS3Location: https://bucket.s3.amazonaws.com/restapi.json
  GlueJob:
    Type: AWS::Glue::Job
    Properties:
      Command:
        ScriptLocation: https://bucket.s3.amazonaws.com/gluejob.py
  Lambda:
    Type: AWS::Lambda::Function
    Properties:
      Code:
        S3Bucket: bucket
        S3Key: lambda.zip
      Runtime: !Sub 'RuntimeVersion'
...

